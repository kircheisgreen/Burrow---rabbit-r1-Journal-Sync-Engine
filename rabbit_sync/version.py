"""
Semantic version for the Burrow - Journal Sync Engine project.

Follows semver.org: MAJOR.MINOR.PATCH.
  - MAJOR: incremented for breaking changes (e.g. incompatible profile
    JSON format changes, like the Set-1 -> VirtualKey scancode switch).
  - MINOR: incremented for backward-compatible new features.
  - PATCH: incremented for backward-compatible bug fixes.
"""

# Prompt semantic version: v0.16.5
__version__ = "0.16.5"